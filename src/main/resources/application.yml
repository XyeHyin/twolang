spring:
  datasource:
    url: jdbc:mysql://localhost:3306/twolang?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123400
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # 可选值: create, update, validate, none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    poker:
      deck-type: standard  # 可选值: standard 或 short